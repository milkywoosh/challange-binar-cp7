/*

function add(x) { return x * 2 };
const cek = (a, b) => {
    const adds =  add(a)
    return { "a": adds, "b": b }
}

// console.log(cek(2,3));


const express = require('expresss');
const app = express();

app.use(express.urlencoded({extended: true}));
app.use(express.json())



const PORT = process.env.PORT || 4320;

app.listen( () => {
    console.log(`currently listening to ${PORT}`)
})


const xxx = require('./tes');

module.exports = {
    anyfunc : (a, b) => {
        return a*b;
    }

    anyOtherFunc: (a, b) => {
        return a*b;
    }
}

const exportingFunc = require('./trial');

exportingFunc.anyfunc(1,2);


exports.anyfunc = (paramX, paramY) => {
    return paramX, paramY;
}

const exportinggg = require('./trial')
*/



console.log( '' == false)